---
- name: Converge
  hosts: all
  tasks:
    - name: Include ansible-role-cdm-certificates
      ansible.builtin.include_role:
        name: ansible-role-cdm-certificates
      vars:
        cdm_certificates_bucket_name: "{{ lookup('env', 'THIRD_PARTY_BUCKET') }}"
